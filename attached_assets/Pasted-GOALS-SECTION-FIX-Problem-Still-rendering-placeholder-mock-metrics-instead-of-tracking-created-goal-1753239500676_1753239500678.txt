GOALS SECTION FIX
Problem: Still rendering placeholder/mock metrics instead of tracking created goals.

🛠️ Steps to Fix:
Connect to Real Goal Data:

Import real-time goal data using an API call:

ts
Copiar
Editar
const { data: goals } = useQuery(["goals"], fetchUserGoals);
Make sure fetchUserGoals() fetches only active goals and returns their progress status.

Calculate Progress Based on KPI:

Match the goal’s metric (revenue, unitsSold, profit, profitMargin) against the current KPI from Dashboard state or database.

Compute completion percentage:

ts
Copiar
Editar
const progress = (currentMetricValue / goal.targetValue) * 100;
Replace Placeholder:

Replace the hardcoded:

tsx
Copiar
Editar
Overall Progress: 79%
Revenue Goal: $388K / $500K
with dynamic values:

tsx
Copiar
Editar
{goalProgress}%
{formatCurrency(currentRevenue)} / {formatCurrency(goal.targetValue)}
✅ RECENT ACTIVITY SECTION FIX
Problem: Still shows hardcoded recent actions like “restocked 500 units”.

🛠️ Steps to Fix:
Fetch Activity Log from API:

Backend should log events like:

product_added

sale_recorded

inventory_alert_triggered

goal_created

goal_achieved

Example API call:

ts
Copiar
Editar
const { data: activities } = useQuery(["activityLog"], fetchActivityLog);
Format Events:

ts
Copiar
Editar
activities.map((event) => {
  return {
    type: event.type,           // success | warning | info
    message: event.message,     // “Low stock alert on X”
    timestamp: event.createdAt, // "2 hours ago"
  };
});
Replace the Static List:
Instead of:

tsx
Copiar
Editar
“Product Wireless Earbuds Pro restocked”
“Low stock alert for Fitness Tracker X1”
Render dynamically:

tsx
Copiar
Editar
{activities.map(a => (
  <ActivityItem key={a.id} type={a.type} message={a.message} time={formatTimeAgo(a.timestamp)} />
))}
🔄 Optional Backend Updates (if needed)
Make sure your backend is logging events into a collection/table like:

bash
Copiar
Editar
ActivityLog {
  id,
  type,
  message,
  createdAt,
  userId,
}
And your /api/activity endpoint returns the latest 5–10 entries.

✅ Result
After this fix:

Your Goal Progress will track active goal KPIs.

Your Recent Activity will display true actions/events.

Your Dashboard becomes fully real-time and personalized